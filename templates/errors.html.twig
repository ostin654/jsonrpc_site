{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col p-3 mt-5">
                <h1>{{ title }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col p-3">
                <p>Errors:</p>
                <ul>
                {% for violation in violations %}
                    <li>{{ violation.message }}</li>
                {% endfor %}
                </ul>
                <p>You can generate new page UUID here -> <a href="https://www.uuidgenerator.net/" target="_blank">uuidgenerator.net</a> and paste it into page URL.</p>
                <p>Or if you know existing page UUID, you can use it.</p>
            </div>
        </div>
    </div>
{% endblock %}
